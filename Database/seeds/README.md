Admin seed data is created by `Backend/src/server/jobs/seed-admin.ts`.

```bash
npm run db:seed
```

Required Backend environment variables:

- `SEED_ADMIN_EMAIL`
- `SEED_ADMIN_PASSWORD`
- `SEED_ADMIN_NAME`
