NODE_ENV=production
# Public site URL (same domain as Backend APP_URL)
APP_URL=https://YOUR_DOMAIN
# Backend API reachable from the Next server (VPS localhost or private IP)
API_URL=http://127.0.0.1:5000
API_PREFIX=/api/v1

# Must match Backend secrets
HMAC_SIGNING_SECRET=replace-with-64-char-hmac-signing-secret-value-not-a-real-secret
SESSION_SECRET=replace-with-64-char-session-secret-value-not-a-real-secret
SESSION_TTL_SECONDS=604800
